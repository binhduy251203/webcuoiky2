@model webcuoiky2.Models.NhaCungCap

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}
<style>
    .text-primary {
        font-weight: bold;
        text-transform: uppercase;
    }

    /* Card tổng quan */
    .card {
        border-radius: 10px;
        width:70%;
        margin-left:15%;
        
    }

    /* Header card */
    .card-header {
        font-size: 1.2rem;
        text-transform: uppercase;
        font-weight: bold;
    }

    /* Nội dung card */
    .dl-horizontal dt {
        color: #343a40;
        font-size: 1rem;
    }

    .dl-horizontal dd {
        font-size: 1rem;
        margin-bottom: 0.5rem;
    }

    /* Nút hành động */
    .btn {
        transition: all 0.3s ease;
    }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
</style>
@*<h2>Details</h2>

    <div>
        <h4>NhaCungCap</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.TenNhaCungCap)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.TenNhaCungCap)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.DiaChi)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DiaChi)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.SoDienThoai)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.SoDienThoai)
            </dd>

        </dl>
    </div>
    <p>
        @Html.ActionLink("Edit", "Edit", new { id = Model.MaNhaCungCap }) |
        @Html.ActionLink("Back to List", "Index")
    </p>*@
<h2 class="text-center text-primary mt-4">Chi Tiết Nhà Cung Cấp</h2>

<div class="card mt-4 shadow-sm">
    <div class="card-header bg-info text-white">
        <h4 class="mb-0">Thông Tin Nhà Cung Cấp</h4>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3 font-weight-bold">Tên Nhà Cung Cấp</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.TenNhaCungCap)</dd>

            <dt class="col-sm-3 font-weight-bold">Địa Chỉ</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.DiaChi)</dd>

            <dt class="col-sm-3 font-weight-bold">Số Điện Thoại</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.SoDienThoai)</dd>
        </dl>
    </div>
    <div class="card-footer text-right">
        @Html.ActionLink("Chỉnh Sửa", "Edit", new { id = Model.MaNhaCungCap }, new { @class = "btn btn-warning mr-2" })
        @Html.ActionLink("Quay Lại Danh Sách", "Index", null, new { @class = "btn btn-secondary" })
    </div>
</div>
